{
  "name": "lab7-multimedios",
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpx servor src/ index.html 1234 --reload",
    "build": "echo \"No build needed\"",
    "predeploy": "echo \"Preparando deploy...\"",
    "deploy": "gh-pages -d src"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0"
  }
}